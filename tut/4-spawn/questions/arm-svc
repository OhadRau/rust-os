The synchronous handler for 64-bit ELx. The address of the exception vector that
gets called is VBAR_EL1+0x400 since it's running AArch64 and originating
from a lower level (EL0) and going up to EL1 (the kernel). Note that this
is a virtual address.